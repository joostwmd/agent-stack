# Custom Hook Tests

Tests for custom hooks with logic (e.g. data fetching wrappers, derived state).

- Render hooks with `renderHook` from Testing Library.
- Mock dependencies per hook contract; avoid testing TanStack Query internals.
