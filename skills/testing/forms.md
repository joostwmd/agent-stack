# Form Tests (RHF + Zod + Shadcn)

Form tests for React Hook Form + Zod + Shadcn Form composition. See `skills/frontend/shadcn/rules/forms.md` for form patterns.

- Cover validation (invalid input, required fields), submit happy path, submit error path.
- Use `renderWithProviders` (project-context defines this).
